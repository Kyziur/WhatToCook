# Generated by Komodo Resource Sync for production; do not create or commit deploy/.env.
# POSTGRES_PASSWORD is supplied only by the homelab BWS workload wrapper.
DATA_ROOT=/srv/whattocook
POSTGRES_DB=whattocook
POSTGRES_USER=whattocook
ASPNETCORE_ENVIRONMENT=Production
SEED_DATA_ENABLED=false
DOCKER_PLATFORM=linux/arm64/v8
# Optional homelab overrides; omit when Compose defaults match.
# OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
# HTTP_PROXY=http://http-proxy:8888
# HTTPS_PROXY=http://http-proxy:8888
# NO_PROXY=localhost,127.0.0.1,api,postgres,*.home.arpa,192.168.0.0/16
